<!DOCTYPE html>
<html lang="en">
  <head>
    <!-- Generated from the 11ty Netlify Jumpstart: https://github.com/5t3ph/11ty-netlify-jumpstart -->
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>{{ title }} | {{ meta.siteName }}</title>
    <meta name="description" content="{{ description }}" />
    <link href="{{ '/styles/css/main-stage.css' | url }}" rel="stylesheet" />
    <link rel="icon" href="{{ '/favicon.png' | url }}" />
    <link
      rel="alternate"
      type="application/rss+xml"
      title="RSS Feed for {{ meta.siteName }}"
      href="/feed/"
    />
    <style>
    header {
      padding-bottom: 0;
      min-height: unset;
    }
    main {
      padding-bottom: 10vh;
    }
    {% if page.url == '/styles/' %}
    main {
      padding-top: 0;
    }
    #styles {
      margin: 0;
    }
    #main h2 {
      background-color: unset;
      color: var(--color-primary);
    }
    article .features li {
      background-color:rgba(213, 34, 87, 0.3);
    }
    article .features li span {
      border: none;
    }
    article .features li:nth-child(even) {
      background-color: rgba(58, 3, 255, 0.1);
    }
    article .features li:nth-child(even) span {
      background-color: var(--color-primary);
    }
    article .features a:visited + span::after {
      content: "✅ Viewed!";
      position: absolute;
      bottom: 0;
      left: 50%;
      transform: translate(-50%, 200%);
      width: 100%;
      color: var(--color-text);
    }
    {% endif %}
    </style>
  </head>
  <body class="page">
    <a href="#main" class="skip-link">Skip to main content</a>
    {% include "nav.njk" %}
    <header>
      <div class="container">
        <h1>{{ title }}</h1>
      </div>
    </header>
    {% if page.url == '/styles/' %}
    {% include "features.njk" %}
    {% endif %}
    <main id="main">
      <div class="container">
        <article id="about">
          <section class="container">
            {{ content | safe }}
          </section>
        </article>
      </div>
    </main>
    {% if page.url !== '/styles/' and page.url !== '/support/' %}
    {% include "features.njk" %}
    {% endif %}
    {% include "footer.njk" %}
  </body>
</html>
